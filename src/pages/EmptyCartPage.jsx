import React from "react";
import { Layout, MainEmptyCart } from "../components";

const EmptyCartPage = () => {
  return (
    <Layout>
      <MainEmptyCart />
    </Layout>
  );
};

export default EmptyCartPage;
